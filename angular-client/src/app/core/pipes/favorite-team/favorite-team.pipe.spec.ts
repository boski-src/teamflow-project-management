import { FavoriteTeamPipe } from './favorite-team.pipe';

describe('FavoriteTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
