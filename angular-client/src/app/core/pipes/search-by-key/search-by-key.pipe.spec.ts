import { SearchByKeyPipe } from './search-by-key.pipe';

describe('SearchByKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
